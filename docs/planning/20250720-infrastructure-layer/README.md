# 인프라 레이어 구현 마스터 계획

**작성일**: 2025-07-20  
**모드**: PLAN  
**상위 계획**: [백엔드 데이터 저장 방식 변경](../20250720-backend-data-storage-migration.md) > **중간 단위 1**

## 📍 프로젝트 위치

이 폴더는 **백엔드 데이터 저장 방식 변경 프로젝트**의 첫 번째 중간 단위인 **"인프라 레이어 구현"** 에 대한 상세 계획을 담고 있습니다.

### 전체 프로젝트에서의 위치

```
백엔드 데이터 저장 방식 변경 프로젝트
├── 1. 인프라 레이어 구현 ← 현재 계획
├── 2. 데이터 레이어 구현 (예정)
└── 3. 통합 및 마이그레이션 (예정)
```

## 🎯 목표

Repository 패턴 기반 인프라 구축을 통해 사용자가 워크스페이스 경로를 지정하고 폴더가 생성되도록 합니다.

**완료 기준**: 사용자가 워크스페이스 경로를 지정하고 폴더가 생성됨

---

## 📂 구현 계획 파일들

### **백엔드 구현** (1.1.x)

| 파일명                                                           | 담당 범위                           | 의존성       | 상태 |
| ---------------------------------------------------------------- | ----------------------------------- | ------------ | ---- |
| [1.1.1 경로 관리 유틸리티](./1.1.1-path-utilities.md)            | const.ts, index.ts, connect.ts 전환 | 없음 (기초)  | ✅   |
| [1.1.2 워크스페이스 초기화](./1.1.2-workspace-initialization.md) | 초기화 함수들, 에러 처리            | 1.1.1        | ✅   |
| [1.1.3 IPC 핸들러](./1.1.3-ipc-handlers.md)                      | 백엔드-프론트엔드 통신              | 1.1.1, 1.1.2 | ✅   |

### **프론트엔드 구현** (1.2.x)

| 파일명                                       | 담당 범위                | 의존성 | 상태 |
| -------------------------------------------- | ------------------------ | ------ | ---- |
| [1.2 저장 위치 선택 UI](./1.2-storage-ui.md) | UI 컴포넌트, 사용자 경험 | 1.1.3  | ✅   |

### **통합** (1.3.x)

| 파일명                                               | 담당 범위              | 의존성        | 상태 |
| ---------------------------------------------------- | ---------------------- | ------------- | ---- |
| [1.3 앱 시작 통합](./1.3-app-startup-integration.md) | 시작 플로우, 설정 관리 | 모든 구성요소 | ✅   |

---

## 🏗️ 전체 아키텍처

### 경로 구조 설계

```
# 로컬 앱 데이터
## 영구 데이터 (userData)
~/Library/Application Support/voyl/  (macOS 기준)
└── config.json                       # 앱 설정 (workspacePath 포함)

## 캐시 데이터 (cache)
~/Library/Caches/voyl/               (macOS 기준)
└── cache.db                          # SQLite 캐시 (매번 초기화)

# 사용자 워크스페이스 (사용자 지정 경로)
[사용자 지정 워크스페이스]/
├── nodes/                            # 노드 메타데이터 (향후 추가)
├── content/                          # 긴 본문 파일 (향후 추가)
├── log/                              # 변경 이력 (향후 추가)
└── settings.json                     # 워크스페이스 설정
```

### 함수 책임 분리

```typescript
// 앱 수준 경로 (common/paths)
getCachePath() // ~/Library/.../voyl/cache.db
getConfigPath() // ~/Library/.../voyl/config.json
getWorkspacePath() // 사용자 지정 경로

// 워크스페이스 수준 경로 (models/workspace)
getNodesPath() // [workspace]/nodes/
getContentPath() // [workspace]/content/
getLogPath() // [workspace]/log/
```

### 데이터 흐름

```
첫 실행 시:
App 시작 → 설정 확인 → 없음 감지 → 저장 위치 선택 UI →
경로 검증 → 워크스페이스 초기화 → 설정 저장 → 메인 화면

기존 사용자:
App 시작 → 설정 확인 → 있음 감지 → 워크스페이스 검증 → 메인 화면
```

---

## 🚦 구현 순서 및 진행 상황

### **Phase 1: 기초 인프라** ✅

1. **1.1.1 경로 관리** ✅ - 독립적, 기초 모듈 (계획 완료)
2. **1.1.2 워크스페이스 초기화** ✅ - 1.1.1 완료 후 (계획 완료)

### **Phase 2: 통신 레이어** ✅

3. **1.1.3 IPC 핸들러** ✅ - 1.1.1, 1.1.2 완료 후 (계획 완료)

### **Phase 3: 사용자 인터페이스** ✅

4. **1.2 저장 위치 선택 UI** ✅ - 1.1.3 완료 후 (계획 완료)

### **Phase 4: 통합** ✅

5. **1.3 앱 시작 통합** ✅ - 모든 구성요소 완료 후 (계획 완료)

## 🔗 의존성 관계

```
1.1.1 (경로 관리) ✅
├── 1.1.2 (워크스페이스 초기화) ✅ 
│   └── 1.1.3 (IPC 핸들러) ✅
│       └── 1.2 (저장 위치 UI) ✅
│           └── 1.3 (앱 시작 통합) ✅
```

---

## ⚙️ 설계 원칙 및 주요 결정사항

### **아키텍처 원칙**

1. **함수형 설계**
   - 클래스 대신 순수 함수 사용 (프로젝트 컨벤션 준수)
   - 명확한 책임 분리와 에러 처리

2. **점진적 도입**
   - 기존 시스템과 병행 운영 가능
   - 단계별 검증 및 롤백 지원

3. **사용자 경험 우선**
   - 직관적인 저장 위치 선택
   - 명확한 에러 메시지와 복구 방안

### **주요 설계 결정**
- **Renderer-driven 초기화**: 설정 없을 때 UI에서 워크스페이스 선택 처리
- **DB 항상 초기화**: 다중 기기 동기화로 인한 outdated 캐시 대응
- **Lazy Load 전략**: 캐시 DB는 빈 상태로 시작, 필요시 데이터 로드
- **IPC 채널 간소화**: 복잡한 추천 시스템 대신 단순한 폴더 선택
- **워크스페이스 최소 초기화**: 필수 디렉터리와 기본 설정만 생성
- **단일 객체 패턴**: 모든 함수 인자를 단일 객체로 받아 확장성 확보

---

## ⚠️ 주요 고려사항

- **권한 처리**: 사용자 선택 경로의 읽기/쓰기 권한 확인
- **OS별 차이**: macOS/Windows/Linux 경로 처리 차이
- **에러 복구**: 초기화 실패 시 자동 정리 메커니즘
- **보안**: 시스템 중요 폴더 접근 방지

---

## 📈 계획 상태

**전체 계획 수립**: ✅ 완료 (2025-08-17)  
**주요 아키텍처 결정**: ✅ 완료  
**구현 준비**: ✅ 준비 완료

### **다음 단계**
1. **구현 시작**: 계획된 모듈들을 순서대로 구현
2. **통합 테스트**: 각 단계별 검증
3. **사용자 피드백**: UI/UX 최적화

---

각 파일은 독립적으로 이해 가능하면서도 전체적으로 일관성 있는 구현 계획을 제공합니다.

_마스터 계획 작성일: 2025-07-20_