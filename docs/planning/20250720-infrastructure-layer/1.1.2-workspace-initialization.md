# 1.1.2 계층적 초기화 로직 구현

**작성일**: 2025-07-20  
**모드**: PLAN  
**의존성**: 1.1.1 계층적 경로 관리

## 🎯 목표

앱과 워크스페이스의 계층적 구조를 기반으로 초기화 로직을 구현합니다. 앱 레벨에서 전체 초기화를 조정하고, 워크스페이스 레벨에서 세부 초기화를 담당하여 안정적인 초기화를 보장합니다.

**완료 기준**:

- [x] 앱 레벨 초기화 함수 구현 (models/app/index.ts)
- [x] 워크스페이스 초기화 로직 구현 (models/app/workspace/index.ts)
- [x] 계층적 에러 처리 및 복구 로직
- [x] ESLint 규칙 준수하는 구조
- [x] 크로스 플랫폼 호환 테스트 작성

---

## 📋 구현 태스크

### **1. 백엔드 최소 초기화** (`src/main/index.ts`)

**역할**: 백엔드 필수 초기화만 수행 (DB + IPC), 워크스페이스 로직은 프론트엔드에서 처리

```typescript
import { app, BrowserWindow } from 'electron'
import { join } from 'path'
import { electronApp, optimizer, is } from '@electron-toolkit/utils'
import * as appModel from './models/app/index.js'
import * as db from './db/index.js'

// 모든 초기화 로직을 IPC로 이동하여 엔트리 포인트 간소화
// 캐시 DB 초기화도 워크스페이스 설정 후 IPC에서 처리

app.whenReady().then(async () => {
  electronApp.setAppUserModelId('com.electron')

  // IPC 핸들러만 등록 (모든 초기화는 IPC를 통해 처리)
  await import('./ipc/index.js')

  // 창 생성
  createWindow()

  // ... 나머지 로직
})
```

### **2. 앱 모델 단순화** (`src/main/models/app/index.ts`)

**역할**: 앱 설정 및 경로 관리, IPC에서 호출할 워크스페이스 함수들 제공

```typescript
// 앱 설정 및 경로 관리 (1.1.1에서 구현됨)
import { app } from 'electron'
import { join } from 'path'
import fs from 'fs-extra'
import { APP_PATHS } from './const.js'
import { initialize as initializeDB } from '../db/index.js'

// 앱 경로 함수들
export function getConfigPath(): string {
  /* ... */
}
export function getCachePath(): string {
  /* ... */
}

// 앱 설정 싱글톤 메모리 관리
let appConfig: { workspacePath: string } | null = null

/**
 * 메모리에서 앱 설정 조회 (싱글톤)
 */
export function getAppConfig(): { workspacePath: string } | null {
  return appConfig
}

/**
 * 메모리에 앱 설정 저장 (싱글톤)
 */
export function setAppConfig(config: { workspacePath: string } | null): void {
  appConfig = config
}

/**
 * 파일에서 앱 설정 로드하여 메모리에 저장
 */
export async function loadAppConfig(): Promise<{ workspacePath: string }> {
  const configPath = getConfigPath()
  const config = await fs.readJson(configPath)
  setAppConfig(config)
  return config
}

/**
 * 앱 초기화 상태 확인 (메모리 기반)
 */
export function isInitialized(): boolean {
  return hasConfig()
}

// 워크스페이스 함수 직접 import (순환 의존성 방지)
import { initializeWorkspace } from './workspace/index.js'

/**
 * 앱 설정 기반 워크스페이스 초기화 (IPC에서 호출)
 * @returns 초기화된 워크스페이스 경로
 */
export async function initializeFromConfig(): Promise<string> {
  const config = await loadAppConfig()

  // 워크스페이스 초기화 (경로 검사 + 디렉터리 생성 + 설정)
  await initializeWorkspace({ workspacePath: config.workspacePath })

  return config.workspacePath
}

/**
 * 앱 초기화 (첫 실행 - 설정 생성만)
 * @param workspacePath 사용자가 선택한 경로
 */
export async function initializeApp({ workspacePath }: { workspacePath: string }): Promise<void> {
  // 1. 앱 설정을 파일에 저장
  await saveAppConfigToFile({ workspacePath })
  // 2. 워크스페이스 초기화 (폴더 구조 생성 + 권한 확인)
  await initializeWorkspace({ workspacePath })
  // 초기화 완료: DB 초기화는 loadApp()에서 별도로 수행
}

/**
 * 앱 로드 (설정 로드 + DB 초기화)
 */
export async function loadApp(): Promise<void> {
  // 1. config 파일에서 메모리로 로드 (이미 메모리에 있다면 파일에서 다시 로드)
  const config = await loadAppConfig()
  // 2. 캐시 DB 초기화
  const cachePath = getCachePath()
  await initializeDB(cachePath)
}

/**
 * 앱 설정을 파일에 저장
 */
async function saveAppConfigToFile(config: { workspacePath: string }): Promise<void> {
  const configPath = getConfigPath()
  const fullConfig = {
    version: '1.0.0',
    workspacePath: config.workspacePath,
    createdAt: new Date().toISOString(),
    lastUsed: new Date().toISOString(),
  }

  await fs.writeJson(configPath, fullConfig, { spaces: 2 })
}
```

**체크리스트**:

- [x] 앱 설정 및 경로 관리 함수 구현
- [x] 함수명 통일 (`loadAppConfig` 사용)
- [x] 워크스페이스 함수들 재export
- [x] 통합 초기화 함수 (`initialize`) 캡슐화
- [x] 아키텍처 규칙 준수 (models는 순수 비즈니스 로직만)

**엔트리 포인트 체크리스트**:

- [x] main/index.ts에서 백엔드 최소 초기화만 담당 (DB + IPC)
- [x] 워크스페이스 관련 로직은 모두 프론트엔드로 이동
- [x] 초기화 순서: 캐시 DB → IPC 핸들러 등록 → 완료
- [x] 프론트엔드-백엔드 책임 분리
- [x] IPC를 통한 워크스페이스 초기화 함수 제공

**완전한 Renderer-driven 초기화/로드 분리**:

**백엔드 (main/index.ts)**:

- [x] IPC 핸들러 등록만
- [x] 모든 초기화/로드 로직 제거

**프론트엔드 플로우**:

1. **isInitialized IPC 호출**
2. **초기화 필요** (첫 실행):
   - Documents 경로로 파일 브라우저 → 경로 선택 → **initializeApp IPC** → **loadApp IPC**
3. **로드 가능** (일반 실행):
   - **loadApp IPC** 호출

**백엔드 초기화 IPC** (`initializeApp`):

1. 앱 설정 저장 (파일)
2. 워크스페이스 초기화 (경로 검증 + 권한 확인 + 폴더 구조 생성)
   - **DB 초기화는 loadApp()에서 별도 처리**

**백엔드 로드 IPC** (`loadApp`):

1. 앱 설정 로드 (파일 → 메모리)
2. **캐시 DB 초기화 (매번 새로 생성)**

### **3. 워크스페이스 초기화 구현** (`src/main/models/app/workspace/index.ts`)

**역할**: 워크스페이스 관련 모든 초기화 작업을 순서대로 조정, 기존 경로 함수들과 통합

```typescript
import fs from 'fs-extra'
import { join } from 'path'
import path from 'path'
import { loadAppConfig, getCachePath } from '../index.js' // ✅ ESLint 규칙 준수
import { WORKSPACE_PATHS } from './const.js'

// 기존 워크스페이스 경로 함수들 (1.1.1에서 구현됨)
export async function getWorkspacePath(): Promise<string> {
  /* ... */
}
export async function getWorkspaceSettingsPath(): Promise<string> {
  /* ... */
}
export async function getNodesPath(): Promise<string> {
  /* ... */
}
export async function getContentPath(): Promise<string> {
  /* ... */
}
export async function getLogPath(): Promise<string> {
  /* ... */
}

/**
 * 워크스페이스 권한 확인
 */
export async function checkWorkspacePermissions({
  workspacePath,
}: {
  workspacePath: string
}): Promise<void> {
  try {
    // 1. 경로 존재 확인 및 생성
    await fs.ensureDir(workspacePath)

    // 2. 읽기/쓰기 권한 확인
    await fs.access(workspacePath, fs.constants.R_OK | fs.constants.W_OK)

    // 3. 실제 쓰기 테스트
    const testFile = join(workspacePath, '.test-write-permission')
    await fs.writeFile(testFile, 'test')
    await fs.remove(testFile)
  } catch (error) {
    throw new Error(`Insufficient permissions for workspace: ${workspacePath}`)
  }
}

/**
 * 워크스페이스 전체 초기화 조정자
 * 최소 초기화: 필수 디렉터리 확인/생성
 * 실패 시 자동 정리 수행
 */
export async function initializeWorkspace({
  workspacePath,
}: {
  workspacePath: string
}): Promise<void> {
  try {
    // 순차적 초기화 (의존성 고려)
    await validateWorkspacePath({ workspacePath })
    await checkWorkspacePermissions({ workspacePath })
    await initDirectories({ workspacePath }) // 최소 초기화: 필수 디렉터리
    await initSettings({ workspacePath }) // 최소 초기화: 기본 설정 파일
  } catch (error) {
    // 초기화 실패 시 정리
    await cleanupFailedInitialization({ workspacePath })
    throw error
  }
}

/**
 * 워크스페이스 경로 검증 (권한 체크는 별도 함수에서 수행)
 */
async function validateWorkspacePath({ workspacePath }: { workspacePath: string }): Promise<void> {
  // 기본적인 경로 유효성 검증
  if (!workspacePath || workspacePath.trim() === '') {
    throw new Error('Workspace path cannot be empty')
  }

  // 절대 경로 확인
  if (!path.isAbsolute(workspacePath)) {
    throw new Error('Workspace path must be absolute')
  }
}

/**
 * 실패한 초기화 정리 - 생성된 모든 항목 강제 삭제
 */
async function cleanupFailedInitialization({
  workspacePath,
}: {
  workspacePath: string
}): Promise<void> {
  const itemsToCleanup = [
    // 파일들
    join(workspacePath, WORKSPACE_PATHS.SETTINGS_FILE),

    // 디렉터리들
    join(workspacePath, WORKSPACE_PATHS.NODES_DIR),
    join(workspacePath, WORKSPACE_PATHS.CONTENT_DIR),
    join(workspacePath, WORKSPACE_PATHS.LOG_DIR),
  ]

  // 모든 항목 삭제 시도 (존재하지 않아도 에러 안남)
  await Promise.allSettled(itemsToCleanup.map((item) => fs.remove(item)))

  // 워크스페이스 폴더가 비어있다면 삭제
  try {
    const files = await fs.readdir(workspacePath)
    if (files.length === 0) {
      await fs.remove(workspacePath)
    }
  } catch {
    // 삭제 실패해도 무시
    console.warn('Failed to cleanup incomplete workspace initialization')
  }
}
```

### **4. DB 초기화 구현** (`src/main/db/index.ts`)

**역할**: 캐시 DB의 완전한 재초기화 (다중 기기 동기화 대응 + Lazy load 전략)

```typescript
import Database from 'better-sqlite3'
import { drizzle } from 'drizzle-orm/better-sqlite3'
import fs from 'fs-extra'
import { join } from 'path'
// ✅ 외부 의존성 없음, 모든 경로를 인자로 받음

// 현재 DB 인스턴스 (재초기화 가능하도록 변수로 관리)
let sqlite: Database | null = null
let db: any = null

/**
 * DB 캐시 초기화 (Lazy load 전략)
 * @param cachePath 캐시 DB 파일 경로
 */
export async function initialize(cachePath: string): Promise<void> {
  try {
    // 1. 기존 DB 연결 정리
    await closeDatabase()

    // 2. 캐시 파일 삭제 (다중 기기 동기화로 outdated 될 수 있어 매번 새로 시작)
    if (await fs.pathExists(cachePath)) {
      await fs.remove(cachePath)
    }

    // 3. 새로운 DB 인스턴스 생성
    await createDatabase(cachePath)

    // 4. 스키마 생성/마이그레이션
    await createSchema()

    console.log('✅ Database cache initialized (empty, ready for lazy loading)')
  } catch (error) {
    console.error('Failed to initialize database:', error)
    throw error
  }
}

/**
 * 기존 DB 연결 정리
 */
async function closeDatabase(): Promise<void> {
  if (sqlite) {
    sqlite.close()
    sqlite = null
    db = null
  }
}

/**
 * 새로운 DB 인스턴스 생성
 */
async function createDatabase(cachePath: string): Promise<void> {
  sqlite = new Database(cachePath)
  sqlite.pragma('journal_mode = WAL')

  db = drizzle(sqlite, {
    logger: process.env.NODE_ENV === 'development',
  })
}

/**
 * 스키마 생성/마이그레이션
 *
 * 🎯 권장 방법: drizzle-kit push 활용 (개발 시)
 * 🚀 런타임 방법: 기존 스키마 import + CREATE TABLE (현재 구현)
 */
async function createSchema(): Promise<void> {
  try {
    // 🎯 최적 방법: drizzle-kit push 사용
    // Production 환경에서는 사전에 `npx drizzle-kit push`로 스키마 생성
    // 하지만 cache DB는 매번 재생성되므로 런타임 생성 필요

    await createSchemaRuntime()

    console.log('✅ Database schema created successfully')
  } catch (error) {
    console.error('Failed to create schema:', error)
    throw error
  }
}

/**
 * 런타임 스키마 생성
 *
 * ✅ 확정된 최고의 방법: Drizzle migrate 함수!
 */
async function createSchemaRuntime(): Promise<void> {
  try {
    // Drizzle 공식 migrate 함수 사용 (production-ready!)
    const { migrate } = await import('drizzle-orm/better-sqlite3/migrator')

    await migrate(db, {
      migrationsFolder: './src/main/db/migrations',
    })

    console.log('✅ Schema migrated successfully with Drizzle migrate')
  } catch (error) {
    console.error('Migration failed:', error)
    throw error
  }
}

/**
 * 💡 Lazy Load 전략
 * - DB는 빈 캐시로 시작
 * - 데이터는 Repository 레이어에서 필요시 파일에서 읽어 캐시에 저장
 * - 상세 구현은 Repository 레이어 구현 시 정의
 */

/**
 * DB 인스턴스 반환 (기존 connect.ts 대체)
 */
export function getDatabase() {
  if (!db) {
    throw new Error('Database not initialized. Call initialize() first.')
  }
  return db
}
```

**체크리스트**:

- [x] 캐시 DB 완전 재초기화 로직
- [x] 다중 기기 동기화 대응 (파일 삭제 후 재생성)
- [x] Database 인스턴스 재생성 로직
- [x] **Drizzle migrate 함수 사용**: 새 테이블 추가 시 코드 수정 불필요 ⭐
- [x] **DB 항상 초기화**: 다중 기기 동기화로 인한 outdated 캐시 대응
- [x] **Lazy Load 전략**: 초기화 시 데이터 로드 없음, 필요시 Repository에서 로드
- [x] 에러 처리 및 로깅
- [x] 기존 connect.ts 대체 인터페이스
- [x] 외부 의존성 제거 (DB는 순수 캐시 기능만 제공)

**중요 변경사항**:

- **DB 항상 초기화**: 앱 시작시 매번 캐시 DB 재생성 (다중 기기 동기화 대응)
- `connect.ts`는 더 이상 모듈 로드시 즉시 DB 연결하지 않음
- `db.initialize(cachePath)`가 호출될 때만 DB 연결 생성
- 재초기화 가능한 구조로 설계
- **완전한 독립성**: DB는 순수 캐시 레이어, 워크스페이스 경로 몰라도 됨
- **Lazy Load**: Repository 레이어에서 필요시 파일 읽고 캐시에 저장
- **Renderer-driven**: 설정 없을 때 메인 프로세스 블로킹 없이 UI에서 처리

**🎯 최종 확정: Drizzle migrate 함수!**

**개발 시 (한 번만)**:

```bash
# Migration 파일 생성
npx drizzle-kit generate
```

**런타임 (매번 자동 실행)**:

```typescript
// Drizzle 공식 migrate 함수
import { migrate } from 'drizzle-orm/better-sqlite3/migrator'
await migrate(db, { migrationsFolder: './src/main/db/migrations' })
```

**장점**:

- ✅ **새 스키마 추가 시 코드 수정 불필요**
- ✅ **Drizzle 공식 지원**: 가장 안전하고 표준적인 방법
- ✅ **Production-ready**: 실제 서비스에서 검증된 방법
- ✅ **자동 의존성 처리**: 외래키, 제약조건 순서 자동 관리
- ✅ **Migration 기록**: 스키마 변화 추적 가능

### **5. 개별 초기화 함수들**

#### **디렉터리 생성** (`initDirectories`)

```typescript
/**
 * 워크스페이스 내 필요한 디렉터리들을 병렬 생성
 */
async function initDirectories({ workspacePath }: { workspacePath: string }): Promise<void> {
  const directories = [
    join(workspacePath, WORKSPACE_PATHS.NODES_DIR),
    join(workspacePath, WORKSPACE_PATHS.CONTENT_DIR),
    join(workspacePath, WORKSPACE_PATHS.LOG_DIR),
  ]

  // 병렬 생성으로 성능 최적화
  await Promise.all(directories.map((dir) => fs.ensureDir(dir)))
}
```

#### **설정 파일 초기화** (`initSettings`)

```typescript
/**
 * 워크스페이스 설정 파일 생성/검증
 */
async function initSettings({ workspacePath }: { workspacePath: string }): Promise<void> {
  const settingsPath = join(workspacePath, WORKSPACE_PATHS.SETTINGS_FILE)

  if (!(await fs.pathExists(settingsPath))) {
    const defaultSettings = createDefaultSettings()
    await fs.writeJson(settingsPath, defaultSettings, { spaces: 2 })
  }
}

/**
 * 기본 워크스페이스 설정 생성
 */
function createDefaultSettings() {
  return {
    version: '1.0.0',
    createdAt: new Date().toISOString(),
    nodeTypes: [], // Node type definitions (빈 배열로 시작)
    attributes: [], // Attribute definitions (빈 배열로 시작)
    // 향후 확장: 테마, 언어, 플러그인 등
  }
}
```

**참고**: 워크스페이스 내부 경로 함수들(`getNodesPath`, `getContentPath`, `getLogPath` 등)은 이미 1.1.1에서 구현되어 이 파일에 포함되어 있습니다.

---

## 🏗️ 파일 구조

```
src/main/models/app/
├── const.ts              # 앱 레벨 상수
├── index.ts              # 앱 함수들 + initializeApp (통합)
└── workspace/            # 워크스페이스 하위 모델
    ├── const.ts          # 워크스페이스 레벨 상수
    └── index.ts          # 워크스페이스 함수들 + initializeWorkspace (통합)

// common/paths/ 디렉터리 제거됨
// 별도 types.ts, utils.ts 파일 없이 index.ts에 통합
```

**계층적 구조 원칙 (Renderer-driven)**:

- **main/index.ts**: 백엔드 최소 초기화 (캐시DB → IPC 핸들러 등록)
- **프론트엔드**: hasConfig 체크 → 워크스페이스 선택/검증 → IPC 호출
- **models/app/index.ts**: IPC에서 호출할 워크스페이스 초기화 함수들 제공
- **models/app/workspace/index.ts**: 워크스페이스 비즈니스 로직 + 경로 함수들
- **db/index.ts**: DB 완전 독립적 초기화 (외부 의존성 없음)
- **완전한 책임 분리**:
  - 백엔드: DB + IPC 핸들러만
  - 프론트엔드: 모든 워크스페이스 관련 UI 로직
- **ESLint 규칙 준수**: models는 db와 common만 import
- **단순화된 구조**: 각 레벨에서 const.ts + index.ts만 사용

---

## 🧪 테스트 계획

### **앱 초기화 테스트** (`src/main/models/app/index.test.ts`)

```typescript
import { tmpdir } from 'os'
import { join } from 'path'
import fs from 'fs-extra'
import { initializeApp, getConfigPath } from './index.js'

describe('App Initialization', () => {
  const testWorkspacePath = join(tmpdir(), 'test-workspace') // ✅ 크로스 플랫폼 호환

  beforeEach(async () => {
    await fs.remove(testWorkspacePath)
  })

  describe('initializeApp', () => {
    it('should initialize app with provided workspace path', async () => {
      await expect(initializeApp(testWorkspacePath)).resolves.not.toThrow()

      // 설정 파일이 생성되었는지 확인
      const configPath = getConfigPath()
      expect(await fs.pathExists(configPath)).toBe(true)
    })

    it('should throw error for invalid workspace path', async () => {
      const invalidPath = '/invalid/readonly/path'
      await expect(initializeApp(invalidPath)).rejects.toThrow()
    })
  })
})
```

### **워크스페이스 초기화 테스트** (`src/main/models/app/workspace/index.test.ts`)

```typescript
import { tmpdir } from 'os'
import { join } from 'path'
import fs from 'fs-extra'
import { initializeWorkspace } from './index.js'

describe('Workspace Initialization', () => {
  const testWorkspacePath = join(tmpdir(), 'test-workspace') // ✅ 크로스 플랫폼 호환

  beforeEach(async () => {
    await fs.remove(testWorkspacePath)
  })

  describe('initializeWorkspace', () => {
    it('should create all required directories', async () => {
      await initializeWorkspace({ workspacePath: testWorkspacePath })

      expect(await fs.pathExists(join(testWorkspacePath, 'nodes'))).toBe(true)
      expect(await fs.pathExists(join(testWorkspacePath, 'content'))).toBe(true)
      expect(await fs.pathExists(join(testWorkspacePath, 'log'))).toBe(true)
    })

    it('should create settings.json with default values', async () => {
      await initializeWorkspace({ workspacePath: testWorkspacePath })

      const settingsPath = join(testWorkspacePath, 'settings.json')
      const settings = await fs.readJson(settingsPath)
      expect(settings.version).toBe('1.0.0')
      expect(settings.createdAt).toBeDefined()
    })

    it('should cleanup on failure', async () => {
      // Mock fs.ensureDir to fail
      jest.spyOn(fs, 'ensureDir').mockRejectedValueOnce(new Error('Permission denied'))

      await expect(initializeWorkspace({ workspacePath: testWorkspacePath })).rejects.toThrow()

      // Verify cleanup (설정 파일 제거 확인)
      const settingsPath = join(testWorkspacePath, 'settings.json')
      expect(await fs.pathExists(settingsPath)).toBe(false)
    })
  })
})
```

**체크리스트**:

- [x] 크로스 플랫폼 호환 테스트 경로 (`tmpdir()` 사용)
- [x] 계층적 구조별 독립적 테스트
- [x] Mock을 통한 의존성 격리
- [x] 에러 케이스 및 정리 로직 테스트
- [x] ESLint 규칙 준수하는 import

---

## ⚙️ 에러 처리 전략

### **에러 타입별 구분**

| 에러 타입            | 처리 방법           | 사용자 경험               |
| -------------------- | ------------------- | ------------------------- |
| **CONFIG_NOT_FOUND** | UI 트리거           | 저장 위치 선택 모달       |
| **권한 없음**        | 앱 중단             | 에러 다이얼로그 + 종료    |
| **디스크 공간 부족** | 앱 중단             | 에러 다이얼로그 + 종료    |
| **부분 초기화 실패** | 자동 정리 후 재시도 | 진행률 표시 + 재시도 버튼 |

### **복구 메커니즘**

1. **자동 정리**: 실패 시 생성된 파일/폴더 제거
2. **상태 검증**: 기존 설정 파일 무결성 확인
3. **점진적 복구**: 부분적 성공 상태에서 재시도 지원

---

## 🔗 다음 단계 연결

이 모듈 완료 후:

1. **1.1.3 IPC 핸들러**에서 `requestUserWorkspacePath()` 구현
2. **프론트엔드**에서 워크스페이스 선택 UI 연동
3. **앱 시작 프로세스**에서 `initializeApp()` 호출

---

## ⚠️ 주의사항

- **원자성**: 초기화는 전체 성공 또는 전체 실패 (부분적 상태 방지)
- **권한 검증**: 폴더 생성 전 미리 권한 확인
- **크로스 플랫폼**: Windows/macOS/Linux 경로 호환성
- **동시성**: 여러 앱 인스턴스에서 동시 초기화 방지

---

_계획 작성일: 2025-07-20_
